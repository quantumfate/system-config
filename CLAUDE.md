# CLAUDE.md

Read [AGENTS.md](AGENTS.md). It is the agent entry point for this repo: the
read order into the README's separation of concerns, the `roles_enabled`
mechanism, `docs/hyprfocus.md`, and the per-host convergence contract, plus the
gate (`just check`).

Nothing in this file overrides AGENTS.md.
