# login_shell

Sets the account's login shell.

- **Vars** `login_shell`, `target_user` in `group_vars/all/main.yml`.
- **Tag** `system`, `shell`

Only the `passwd` entry. The shell's own config is `roles/zsh`.
