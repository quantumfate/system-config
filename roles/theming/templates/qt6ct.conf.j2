{% import '_qtfont.j2' as qt -%}
# ~/.config/qt6ct/qt6ct.conf — rendered by roles/theming.
#
# QT_QPA_PLATFORMTHEME=qt6ct makes this file the source of truth for every Qt6
# app AND for the Qt file dialogs that GTK portals hand off to. The previous
# file was a single line of literal "\n" escapes, which qt6ct cannot parse, so
# Qt6 apps were running unstyled with default fonts.
#
# Style comes from Kvantum (the catppuccin Kvantum theme draws the widgets) and
# the palette from the catppuccin colour scheme; do NOT also set
# QT_STYLE_OVERRIDE in the environment — two style selectors fight, and the
# environment one wins over anything chosen here.

[Appearance]
style=kvantum
color_scheme_path={{ target_home }}/.config/qt6ct/colors/{{ theme_qt_colors }}.conf
custom_palette=true
icon_theme={{ theme_icons }}
standard_dialogs=default

[Fonts]
general="{{ qt.qtfont(theme_ui_font, theme_ui_font_size) }}"
fixed="{{ qt.qtfont(theme_mono_font, theme_mono_font_size) }}"

[Interface]
activate_item_on_single_click=1
buttonbox_layout=0
cursor_flash_time=1000
dialog_buttons_have_icons=0
double_click_interval=400
gui_effects=@Invalid()
keyboard_scheme=2
menus_have_icons=false
show_shortcuts_in_context_menus=true
stylesheets=@Invalid()
toolbutton_style=4
underline_shortcut=1
wheel_scroll_lines=3
