# ~/.config/Kvantum/kvantum.kvconfig — rendered by roles/theming.
# Kvantum draws the Qt widgets. This was pinned to a MOCHA theme while every
# other surface was macchiato, which is why Qt windows read as a slightly
# different dark than GTK ones.
[General]
theme={{ theme_kvantum }}
