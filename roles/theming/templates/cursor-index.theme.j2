# ~/.icons/default/index.theme — rendered by roles/theming.
# The X11-era cursor lookup path. Wayland clients take the cursor from the
# compositor and GTK/Qt settings, but XWayland apps and a few toolkits still
# resolve "default" through this file.
[Icon Theme]
Name=Default
Comment=Default cursor theme
Inherits={{ theme_cursor }}
