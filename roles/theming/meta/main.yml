---
galaxy_info:
  role_name: theming
  namespace: local
  author: local
  description: >-
    GTK, Qt, cursor and browser theming — one flavour, one accent, one type
    scale, published to every toolkit and to the XDG portals.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: ArchLinux
      versions:
        - all
  galaxy_tags:
    - theming
    - gtk
    - qt

# The sudo timestamp yay needs is warmed by site.yml's pre_tasks, not by a role.
dependencies: []
