---
# XDG user dirs that xdg-user-dirs-update does not create on its own.

- name: Create XDG user directories
  ansible.builtin.file:
    path: "{{ target_home }}/{{ item }}"
    state: directory
    mode: "0755"
  loop: [Music, Public, Templates, Videos]
